import React from 'react'
import MovieCard from './MovieCard'

const MovieLists = ({dataMovie}) => {
  return (
    <div>
        {dataMovie.map(movie=> <MovieCard dataMovie={movie} key={movie.id}/>)}
    </div>
  )
}

export default MovieLists